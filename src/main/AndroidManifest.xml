<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KarkhanaApp">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.KarkhanaApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".LoginActivity" />

        <!-- OTP Verification -->
        <activity android:name=".OtpVerificationActivity" />

        <!-- Personal Details -->
        <activity android:name=".PersonalDetailsActivity" />

        <!-- Farm Location -->
        <activity android:name=".FarmLocationActivity" />

        <!-- Main App (with Bottom Navigation) -->
        <activity android:name=".MainAppActivity" />

        <!-- Keep old activities for backward compatibility -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SetFarmLocation" android:exported="false" />

        <service
            android:name=".KarkhanaFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>